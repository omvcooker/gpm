--- gpm-1.20.7-27-g1fd1941/src/lib/report-lib.c.0002~	2016-11-14 16:52:50.584365563 +0100
+++ gpm-1.20.7-27-g1fd1941/src/lib/report-lib.c	2016-11-14 16:55:01.465227977 +0100
@@ -24,13 +24,21 @@
 
 #include "headers/message.h"
 
+static inline int gpm_silent() {
+   if ( getenv( "GPM_VERBOSE" ) == NULL ) return 1;
+   return 0;
+}
+
 void gpm_report(int line, const char *file, int stat, const char *text, ... )
 {
    const char *string = NULL;
    int log_level;
    va_list ap;
 
-   if (stat == GPM_STAT_DEBUG) return;
+
+   if ((stat == GPM_STAT_DEBUG) || (gpm_silent() &&
+			   stat != GPM_STAT_OOPS))
+	   return;
 
    va_start(ap,text);
 
